From 90a980e793ab20c340cf83eb71170e2f880dc7e2 Mon Sep 17 00:00:00 2001
From: Zenium Upstream Port <arena-port@zenium.local>
Date: Fri, 4 Sep 2026 18:25:13 +0000
Subject: [PATCH 53/73] drivers-gnss: carry downstream 4.19.325 vendor delta
 onto v5.15.220

Ported mechanically by upstream-port/bin/portclassify.py from:
  Zenium_Kernel 4.19.325 (MTK/OPLUS downstream, squashed snapshot)

2 file(s) in this group.  Every hunk was applied only where its pre-image
matched the target text exactly; classification: PORTABLE (pre-image found),
ALREADY (already upstream, dropped), MANUAL/NEAR/PARTIAL (left for review,
see upstream-port/report/ledger.csv).

See upstream-port/MIGRATION-5.15.md for the audit and the remaining work.
---
 drivers/gnss/Kconfig  | 15 +++++++++++++++
 drivers/gnss/Makefile |  3 +++
 2 files changed, 18 insertions(+)

diff --git a/drivers/gnss/Kconfig b/drivers/gnss/Kconfig
index bd12e3d57..50deac358 100644
--- a/drivers/gnss/Kconfig
+++ b/drivers/gnss/Kconfig
@@ -54,4 +54,19 @@ config GNSS_UBX_SERIAL
 
 	  If unsure, say N.
 
+config GNSS_CMDLINE_SERIAL
+	tristate "Command line test driver for GNSS"
+	depends on SERIAL_DEV_BUS
+	select GNSS_SERIAL
+	---help---
+	  Say Y here if you want to test the GNSS subsystem but do not have a
+	  way to communicate a binding through firmware such as DT or ACPI.
+	  The correct serdev device and protocol type must be specified on
+	  the module command line.
+
+	  To compile this driver as a module, choose M here: the module will
+	  be called gnss-cmdline.
+
+	  If unsure, say N.
+
 endif # GNSS
diff --git a/drivers/gnss/Makefile b/drivers/gnss/Makefile
index 451f11401..1d27659f5 100644
--- a/drivers/gnss/Makefile
+++ b/drivers/gnss/Makefile
@@ -17,3 +17,6 @@ gnss-sirf-y := sirf.o
 
 obj-$(CONFIG_GNSS_UBX_SERIAL)		+= gnss-ubx.o
 gnss-ubx-y := ubx.o
+
+obj-$(CONFIG_GNSS_CMDLINE_SERIAL)	+= gnss-cmdline.o
+gnss-cmdline-y := cmdline.o
-- 
2.39.5

