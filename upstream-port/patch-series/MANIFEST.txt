# Zenium 4.19.325 -> v5.15.220 port series (73 commits + cover letter)
# format-patch output, stored as .eml so the kernel's *.patch gitignore does not swallow them.
#
# apply:    git checkout v5.15.220 && git am patch-series/0[0-9][0-9][0-9]-*.eml
#           (four-digit glob: 0001..0073. `0[0-9][0-9]-*.eml` matches nothing here and a
#            non-matching glob still leaves `git am` succeeding with an empty list - which
#            is how a stale "applied" claim once got into a report. Assert the tree hash.)
# verify:   git rev-parse HEAD^{tree}  ==  ad82b1376943068d31f7f06f223240a7bd0be7a0
#           (measured twice in this workspace: a fresh `git worktree add --detach` at
#            v5.15.220, `git am` rc=0, tree hash byte-identical to the tree that was built)
# build:    make ARCH=arm64 LLVM=1 -k -j2  ->  Image 26,877,960 B (sha256 bf438d69be7a3f41…),
#           vmlinux 37,333,272 B, 7,364 objects, 840 .ko, 0 `error:`, 0 make failures
#           (report/build.json; the device-config rerun that also produces mt6768.dtb,
#            the five dtbo overlays, Image.gz-dtb and pinctrl-mt6768.o is build-27)
# content:  commits 0001-0068  the mechanically portable vendor delta (see ../report/ledger.csv)
#           commit  0069-0072  device round: image packaging kbuild, even device tree
#                              transplant (base DTB + overlays + dt-binding headers),
#                              pinctrl-mt6768 (Paris), devapc-mt6768 5.15 fixes
#           commit  0073       revert of the half-ported MT6397 RTC rework (its 4.19
#                              mt6397-core dependency is not in the port)
# author:   Zenium Upstream Port <upstream-port@zenium.invalid>  (tool-generated)
#
# NOT included by design, and why: 383 of the 417 compatibles in the transplanted device
# tree have no driver in 5.15 (report/dtsport.json) - those are vendor driver transplants
# (Mali DDK, MSDK video, connectivity, imgsensor, OPLUS charging, E-CCCI, ASoC machine),
# tracked per subsystem with file counts in report/subsystem-audit.md and ranked in
# ../MATURITY.md. This series is the source-complete + build-complete part of that work.
