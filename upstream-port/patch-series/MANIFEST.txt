# Zenium 4.19.325 -> v5.15.220 port series (91 commits + cover letter)
# (this header describes the CURRENT tip and is rewritten at publish time by
#  upstream-port/bin/publish.py's checks; every number in it was measured in the round named -
#  it used to read "82 commits" and "must be 81" because the header had been carried forward
#  by hand since 0081, which is the same failure mode that made a cover-letter diffstat stale)
# format-patch output, stored as .eml so the kernel's *.patch gitignore does not swallow them.
#
# apply:    git checkout v5.15.220 && git am $(ls patch-series/*.eml | grep -v cover | sort)
#           (assert the count first: `ls patch-series/*.eml | grep -v cover | wc -l` must be 91 -
#            an empty glob makes `git am` "succeed" with nothing applied, which is how a stale
#            "applied" claim once got into a report here)
#           exclude the cover from the am list too: it carries no diff, and `git am` on it stops the
#           whole sequence with "Patch is empty" on patch 0000 (measured while gating 0080)
# verify:   git rev-parse HEAD^{tree}  ==  3483759c24eb022373a5290523933b61bbd7ac62  (0091 tip)
#           published prefixes, re-derived by the same command in the 0091 round, both exact
#           (the older ones below are the rounds' own measurements named beside them, carried
#           forward and not re-run, because 0091 touches no file that any of them contains):
#             0001-0090 -> 7fbaf8257bfa9a33b6909c6ea4cfc1f2b17269ed   (0091 round)
#             0001-0089 -> 7320325c38fdc188de726f3ba658d0f6b80e7eb6   (0090 round)
#             0001-0088 -> 1a7cf42b066c5379a93cea37fa22a41a4bd9d4c3
#             0001-0087 -> deba5bd29ec656ecb9b542837198cccc76cc5a09
#             0001-0085 -> 01e8b1eae19a33684b8769415ba16647d1de452a
#             0001-0084 -> 3fa1c650082e917773ac00d2190befb35d575572
#             0001-0083 -> 1bbd779ea9182f344c9e231621bca0ae8b715dae
#             0001-0081 -> d24f24ea02f61b648cb4a62d2fab497a15eb5e7d
#           (one command, in the landing tree: `python3 upstream-port/bin/publish.py --tree <tree>
#            --expect-tree <tip> --prev-tree <0084> --verify-only`, which does the fresh worktree,
#            the `git am`, the tree compare and the dirty check - and refuses if the count is wrong)
#           the current build gate for the display slice is in report/build.json (gate
#            l2_disp_record_publish49, i.e. the 0091 tip; 0090's is l2_path_layer_publish48,
#            0089's l2_pmic_dsv_publish47, 0088's l2_slot_pool_publish46) and
#            report/display-bringup-plan.md 11.17 (11.16 is the 0090 round, 11.11 the 0089 round);
#            (0089's gate was re-run after a sandbox reset in a tree rebuilt from these .eml files
#             alone - gate l2_pmic_dsv_reverify48 - and reproduced 499/78 and the 39-symbol census,
#             with the only byte differences coming from git-describe in the version string)
#            the include-regression history stays in report/l2-videox-include-regression.md; the whole-tree
#            link+image state for 0082-0087 is in gates l2_wholetree_survey45 / l2_wholetree_link45 /
#            gate_published_0087, and the numbers further below (Image 27,035,656 B etc.) are the
#            recorded state at 0081 under a config that is no longer recoverable - they are kept as
#            history, not as the current build state
# build:    RECORDED AT 0081 (build-37). Device config (dev/even.fragment + dev/even-hardware.fragment), via
#           `logs/run.sh build-NN` (sources tools/bin64/env.sh; keep the runner OUT of git):
#           Image 27,035,656 B, Image.gz 10,647,924 B, Image.gz-dtb 11,141,441 B (appended payload 493,517 B
#           = exactly the six blob files), mt6768.dtb 122,474 B (sha 34a7e6b5..., unchanged since build-33), dtbo.img
#           371,235 B (sha 38fe681f..., identical to build-36), out/boot.img 11,268,096 B (sha e478953545be5f27...),
#           7,379 objects, 840 .ko, 0 `error:`, 0 make failures, 0 warnings  (logs/build-37.log; clk + PMIC chain + AUXADC pair +
#           + mt6358 supply binding + mtk-sd + the vendor SMI substrate + M4U + its display client: `nm vmlinux | grep -cE
#           '^T (mtk_smi_clk_enable|mtk_smi_dev_get|mtk_smi_conf_set|smi_bus_)'` = 6 with 7
#           m4u side of the same interface: `nm vmlinux | grep -cE " T m4u_| T MTK_M4U| t m4u_probe"` = 131,
#           and the 27 files of commit 0080 are sha256-identical between the built tree and the
#           series worktree, so the audited tree and the published tree are one tree (measured, not
#           assumed, because the built tree is not a git checkout of the series)
#           __ksymtab_* exports, and `sha256sum` of the built mt6768.dtb is unchanged from build-33,
#           i.e. the SMI round edited no device tree;
#           mt6358 supply binding + mtk-sd included: `nm --defined-only vmlinux | grep -cE
#           'mt635x_auxadc|mt6577_auxadc|mt6358_regulator|msdc_|mtk_sd'` = 80, and
#           `nm vmlinux | grep -c pmic_auxadc_chip_init` = 0 - the deliberately-not-ported
#           BSP glue leaves no dangling reference)
#           DTB: the .dtb appended to Image.gz-dtb is now byte-identical to the one `make dtbs`
#           builds (122,474 B, 413 compatible-bearing nodes / 450 compatible entries) - commit 0077+0078;
#           the packaged image
#           carries 6 FDT blobs (mt6768.dtb + 5 .dtbo), and the DTB the audits read is inside it
#           at offset 10,603,132, so report/clkaudit.json and hardware-enablement.json describe
#           the image we ship, not a different build of the same source
#           `nm --defined-only vmlinux | grep -ciE 'clk_mt6768|mtk_is_mtcmos_enable|pg_'` = 238,
#           `nm vmlinux | grep -c pwrap` = 38 with `pwrap_mt6768` present)
#           `nm vmlinux | grep -cE ' T disp_(m4u_init|m4u_callback|mva_map_kernel|allocate_mva)| T config_display_m4u_port
#             | T disp_hal_allocate_framebuffer| T disp_helper_(get_option|option_init)'` = 9, and ddp_m4u.o's five M4U
#           references resolve into m4u/2.0/m4u.o and m4u/mt6768/m4u_hw.o (bind audit 34/25/5/315 with an EMPTY row
#           diff vs build-36). Rejected attempt kept: a mid-round `./build.sh configure` wiped the hand-enabled
#           symbols (Image 27,015,176, Image.gz-dtb == Image.gz, i.e. no DTB appended) - logs/build-37a-rejected.log;
#           portwork/configs/apply.sh now carries the recipe and fails the build if any symbol is missing.
#           (report/build.json is build-37 and report/clkaudit.json is measured on the packaged
#            DTB (234 clock refs, 234 resolving; run it as
#            `bin/clkaudit.py --dtb arch/arm64/boot/dts/mediatek/mt6768.dtb --driver
#            drivers/clk/mediatek/clk-mt6768.c drivers/clk/mediatek/clk-mt6768-pg.c --header
#            include/dt-bindings/clock/mt6768-clk.h --require-fresh`, since the MTCMOS provider lives
#            in the second file); build-evidence.md carries the pass-by-pass log index)
# content:  commit  0081       video/mt6768: the display-side M4U client glue - ddp_m4u.c (249 of the vendor's 400
#                              lines) + the disp_helper option table, 10 files / 1,116 lines (video/ set; 12 files touched including drivers/misc/{Kconfig,Makefile}), CONFIG_MTK_DISP_M4U=y,
#                              obj-y with no probe and no initcall so it binds nothing and changes no behaviour; all
#                              41 ion_ references removed with the seven disp_ion_*() wrappers the vendor's own
#                              MTK_FB_ION_SUPPORT gate already compiled out, ddp_reg.h/ddp_dump.c/ddp_info.c/
#                              display_recorder.c left out with reasons in the source, and the traced sequence
#                              executed on the host against a recording stub (43 checks, 0 failures, ABI byte-identical
#                              to 4.19) - report/display-m4u-client.md, KNOWN-ISSUES.md 12
#           commits 0001-0068  the mechanically portable vendor delta (see ../report/ledger.csv)
#           commit  0069-0072  device round: image packaging kbuild, even device tree transplant
#                              (base DTB + overlays + dt-binding headers), pinctrl-mt6768 (Paris),
#                              devapc-mt6768 5.15 fixes
#           commit  0073       revert of the half-ported MT6397 RTC rework (its 4.19 mt6397-core
#                              dependency is not in the port)
#           commit  0074       clk: mt6768 clock provider + MTCMOS power gating, audited against the
#                              board DTB (report/clkaudit.json)
#           commit  0075       soc/mediatek: pwrap for MT6768 + MT6358 node-name alias, which is
#                              what makes MFD_MT6397 / REGULATOR_MT6358 / RTC_DRV_MT6397 bindable
#                              (register-level evidence: report/hardware-enablement.md, PMIC section)
#           commit  0077       arm64: boot: keep the board's DTS_CPPFLAGS in scope for the
#                              Image-dtb / Image.gz-dtb packaging path, so the DTB in the image is
#                              the DTB we audited (KNOWN-ISSUES 8.1, was: two different .dtb files
#                              per target, image carried the node-poorer 89,053 B one)
#           commit  0080       misc/mediatek/m4u: the MT6768 M4U v2.0 engine (16 files, 10,896 lines)
#                              built on the vendor SMI API, no ION, no dma-buf heaps; six annotated
#                              5.15 API adaptations, mmprofile tracing compiled out through the BSP
#                              header's own no-op branch, 32-bit compat ioctls selected off because
#                              this base has no fs/compat.c, m4u_hw.c:1723 kept as-is (report/m4u-port.md,
#                              KNOWN-ISSUES 11); mediatek,m4u measured ENABLED against the packaged DTB
#           commit  0079       memory/mtk-smi: Kconfig symbol renamed to CONFIG_MTK_SMI_EXT (M4U's own
#                              #ifdef spelling: smi_public.h's #else no-ops smi_bus_prepare_enable() under
#                              any other name), smi_public.h carried over, smi_mm_first_get() added; M4U's
#                              ION dependency measured as vestigial (report/m4u-ion-audit.md)
#           commit  0078       drivers/memory: the vendor MT6768 SMI substrate (BSP CONFIG_MTK_SMI_EXT
#                              implementation + the two smi_bus_* wrappers), CONFIG_MTK_SMI_MT6768,
#                              binds this board's smi_common + 5 larbs with ZERO DT edits; BWC/mmdvfs/
#                              sspm/emi and smi_register() are documented as not-ported, not stubbed
#           commit  0076       iio/regulator/mmc: SoC + PMIC AUXADC providers, per-rdev of_node so
#                              PMIC supply phandles resolve (41/42 DT children match), and the
#                              MT6768 eMMC host = the root device; I2C deliberately NOT enabled
#                              because the board DT's i2c nodes are not adapters (KNOWN-ISSUES 8.4)
# author:   Zenium Upstream Port <upstream-port@zenium.invalid>  (tool-generated)
#
# NOT included by design, and why: most compatibles in the transplanted device tree have no driver
# in 5.15 - 315 of the 349 distinct compatibles across the 413 compatible-bearing nodes per
#                              report/hardware-enablement.json (its
# per-row `driver=` column is the trustworthy part; aggregate counts are only comparable within a
# single invocation of the tool, see ../KNOWN-ISSUES.md 7.4). Those are vendor driver transplants
# (Mali DDK, MSDK video, connectivity, imgsensor, OPLUS charging, E-CCCI, ASoC machine), tracked
# per subsystem with file counts in report/subsystem-audit.md and ranked in ../MATURITY.md. This
# series is the source-complete + build-complete part of that work.

0082 cmdq: revert the partial vendor carry into the mailbox driver
  - files: drivers/mailbox/mtk-cmdq-mailbox.c, drivers/mailbox/Makefile,
    include/linux/mailbox/mtk-cmdq-mailbox.h -> v5.15.220 state (mailbox.c keeps its 0034 delta)
  - why: 0034's +460-line carry into the mainline driver references cmdq_err, CMDQ_DRIVER_NAME,
    cmdq_msg and struct cmdq.base_pa, none of which exist in this series; with
    CONFIG_MTK_CMDQ_MBOX=y the tree did not build, and no fragment enabled that symbol for builds
    33-37, so the breakage was invisible.
  - gate: make -j2 ARCH=arm64 CROSS_COMPILE=aarch64-buildroot-linux-gnu- drivers/mailbox/
    drivers/soc/mediatek/ -> rc=0 (mtk-cmdq-mailbox.o 123,688 B, mtk-cmdq-helper.o 92,776 B)
  - reproduction: git am of all 82 onto v5.15.220 (0996e0926) -> rc=0, applied=82, dirty=0,
    tree d420535584575cf6979c9102341614cdef4a9739; the 81-patch prefix reproduces the previously
    published tree d24f24ea02f61b648cb4a62d2fab497a15eb5e7d exactly, so 0001-0081 content is
    unchanged - only the Subject index went NN/81 -> NN/82 (81 files, one line each).
  - numstat vs v5.15.220 after 0082: 852 files, +91,260 / -2,382 (was 855 / +91,713 / -2,394;
    the delta is the reverted mailbox lines).

0083 soc: mediatek: add the four CMDQ client entry points the MT6768 display path needs
     Files: drivers/soc/mediatek/mtk-cmdq-helper.c, include/linux/soc/mediatek/mtk-cmdq.h. The four
     names are what the 21 objects this board builds from video/mt6768/dispsys/Makefile call and
     v5.15.220 lacks: cmdq_dev_get_event (7 callsites), cmdq_pkt_wait_no_clear (3),
     cmdq_pkt_flush (2), cmdq_pkt_flush_threaded (1). Counts come from grep -ohE over those files, not
     from the whole vendor tree; ddp_disp_bdg.o is gated on CONFIG_MACH_MT6768, not on
     CONFIG_MTK_MT6382_BDG, so its callsites are live.
     Placement is mainline's own helper, not a new file and not a parallel header: struct
     cmdq_instruction and the CMDQ_CODE_*/CMDQ_WFE_* encodings are file-private to
     drivers/soc/mediatek/mtk-cmdq-helper.c, so a vendor-owned twin could not reuse them, and one
     client API in the tree is what keeps DRM_MEDIATEK users and this port from diverging.
     Encoding honesty on cmdq_pkt_wait_no_clear: it introduces NO new instruction. 4.19.325 appends a
     WFE with arg_b = CMDQ_WFE_WAIT | CMDQ_WFE_WAIT_VALUE and arg_c zero; in this tree
     CMDQ_WFE_OPTION is exactly that pair (include/linux/mailbox/mtk-cmdq-mailbox.h:19-31) and
     cmdq_pkt_wfe() ORs in CMDQ_WFE_UPDATE only when clear=true, so cmdq_pkt_wfe(pkt, ev, false) is
     bit-identical. Both sources were read to establish this; an earlier note in this effort claiming
     mainline "cannot express wait-no-clear" was wrong and is retracted here as well as in the plan.
     Documented mechanism difference on cmdq_pkt_flush: the vendor's blocking branch
     (4.19.325 mtk-cmdq-helper.c:2073, selected because CONFIG_MTK_CMDQ_MBOX_EXT is unset on even -
     even_defconfig:1807) waits on a completion signalled by the EOC callback. Reusing that here
     would not block at all: this tree's cmdq_pkt_flush_async() calls mbox_client_txdone() itself
     right after mbox_send_message() ("We can send next packet immediately, so just call txdone"), so
     the completion is already done at submit time. The port finalizes and blocks on mbox_flush(),
     which cmdq_mbox_flush() implements by draining the thread's pending list, and that returns
     -ETIMEDOUT where stock would have hung. CMDQ_PKT_FLUSH_TIMEOUT_MS 1000 is a policy choice, not a
     measured value, and is labelled so in the header. cmdq_pkt_flush_threaded keeps stock's contract.
     cmdq_dev_get_event resolves gce-event-names, gce-events and #gce-event-cells, which this port's
     device tree does not carry, so on even it currently returns an error; no default event number is
     invented, and the display round must supply the DT properties.
     Still open, deliberately: cmdq_pkt_sleep and cmdq_pkt_sleep_by_poll (this tree's opcode enum has
     no SLEEP, LOGIC or plain READ code, and the vendor path needs the GPR/TPR encoder plus
     cmdq_mbox_get_base_pa(); transcribing those words from the vendor, not from analogy, is the next
     stage), and cmdq_register_device, which stays unshimmed - its only live callsite is to be
     rewritten against cmdq_dev_get_client_reg() when that file is ported.
     Gates on exactly this tree: fresh worktree of v5.15.220 (0996e0926), `git am` of 0001-0083 ->
     rc=0, applied=83, dirty=0, tree 1bbd779ea9182f344c9e231621bca0ae8b715dae == series tree,
     cross-tree git diff empty; 0001-0081 prefix -> d24f24ea02f61b648cb4a62d2fab497a15eb5e7d
     (unchanged content). Device config (defconfig + configs/apply.sh + MAILBOX/MTK_INFRACFG/
     MTK_CMDQ_MBOX=y), make prepare rc=0, then make drivers/mailbox/ drivers/soc/mediatek/ rc=0 with
     zero errors and zero warnings; mtk-cmdq-mailbox.o 123,688 B, mtk-cmdq-helper.o 104,352 B (was
     92,776 B before 0083); nm shows cmdq_dev_get_event/cmdq_pkt_flush/cmdq_pkt_flush_threaded/
     cmdq_pkt_wait_no_clear each exactly once as `T`, and no duplicate definition of any cmdq_ symbol
     anywhere in the objects built so far.
     NOT on hardware. Nothing in the display path is functional; L2 stays closed until the sleep pair
     and the callsite replacements are landed and build-verified.


CORRECTION + CLOSURE (stage 3, 2026-09-05) - applies to the 'still open' wording in the 0082/0083
blocks above; published .eml bodies are deliberately NOT rewritten because the .eml set is the build.
  The sleep family is not a requirement of this board, measured rather than assumed. New tool
  bin/cmdqcensus.py strips C comments and string literals, then resolves each callsite's enclosing
  #ifdef chain against even_defconfig:
    cmdq_pkt_sleep          1 hit,   0 live  - the sole occurrence (ddp_dsi.c:7099) is inside /* */
    cmdq_pkt_sleep_by_poll  4 hits,  0 live  - all 4 callsites AND the vendor definition
                                            (mtk-cmdq-helper.c:1329-1372) AND its declaration
                                            (mtk-cmdq.h:412-414) sit under #ifdef
                                            CONFIG_MTK_MT6382_BDG, which even_defconfig leaves unset
    cmdq_pkt_timer_en       0 hits   - the only other cmdq_mbox_get_base_pa() consumer
    cmdq_pkt_poll_gpr_check body wholly inside #if IS_ENABLED(CONFIG_MACH_MT6885) - empty on MT6768
  So no cmdq_pkt_sleep, no cmdq_pkt_sleep_by_poll, no cmdq_mbox_get_base_pa(), no GPR/TPR encoder and
  no new opcodes enter the tree: unreachable code with no callsite is the speculative shim this port
  forbids. The earlier 'sleep pair deferred pending transcription' framing was wrong in kind - it
  treated grep hits as requirements.
  Encodings are recorded as measured numbers instead: tests/cmdq_words_host_check.c transcribes the
  vendor struct cmdq_instruction (arg_c:16/arg_b:16/arg_a:16, s_op:5 + arg_c_type/arg_b_type/
  arg_a_type, op:8), cmdq_pkt_instr_encoder(), v5.15.220's union struct and both trees' WFE builders.
  Output report/cmdq-words-check.txt: 48 comparisons, 0 mismatches. cmdq_pkt_wait_no_clear(ev) and
  cmdq_pkt_wfe(ev, false) agree on every event 0..0x3fe (word 2000000000008001 at ev=0) and both
  reject ev>=0x3ff, so 0083's bit-identity claim is machine-checked. Vendor-only constants captured:
  CMDQ_CODE_LOGIC 0xa0, JUMP_C_ABS 0xb0, JUMP_C_REL 0xb1, WAIT_NO_CLEAR 0x22 (the record layer's own
  opcode - the helper uses WFE with a different arg_b), TPR_ID 56, GPR_CNT_ID 32, CPR_TPR_MASK 0x8000,
  CPR_SLP_GPR_MAX 0x8003, EVENT_GPR_TIMER 994, TPR_MASK 0xD0, TPR_TIMEOUT_EN 0xDC, US_TO_TICK(t)=t*26.
  L1 closed: all 11 live display-side CMDQ names are declared in the ported headers; device-config
  make of drivers/soc/mediatek/ + drivers/mailbox/ + drivers/memory/ rc=0, 0 errors, 0 warnings,
  73 objects; nm shows 12 cmdq_ names each defined exactly once, the 6 vendor-only names at 0
  definitions, and no duplicate among the 45 cmdq_ definitions in the built tree.
  cmdq_register_device: not shimmed, and the rewrite is 18 lines not 1 - ddp_disp_bdg.c:3030 assigns
  disp_bdg_gce_base and :3099-3165 pass it to 14 cmdq_pkt_write() + 3 cmdq_pkt_poll() calls, all in
  the 0x0002xxxx window; the verified 5.15 form is cmdq_dev_get_client_reg() +
  cmdq_pkt_write(pkt, reg.subsys, reg.offset, value), and our transplanted mt6768.dts retains the
  gce-subsys/gce-client-reg properties (3 matching lines, same count as the vendor file).
  L2 display work is unblocked. Remaining CMDQ-adjacent L2 items are the v3 record layer
  (cmdq_op_init_variable/write_reg/read_reg/if/end_if/assign/and) and
  cmdq_mdp_set_resource_callback - a port-or-rewrite decision in L2, not an L1 gap.


0084 video/mt6768: land the CMDQ-free dispsys core (14 objects) and its header substrate
     L2 slice 1. Files: 14 .c from video/mt6768/dispsys, 46 headers (the measured include closure:
     ddp_reg.h + its 8 block register headers, ddp_info/mutex/path/dump/debug/mmp/m4u/hal/log headers,
     disp_session.h, disp_drv_log.h, display_recorder.h, primary_display.h, ...), and 7 v3 CMDQ headers.
     Carried objects (14): ddp_clkmgr ddp_color_format ddp_debug ddp_drv ddp_dump ddp_info ddp_irq
     ddp_m4u ddp_manager ddp_met ddp_mutex ddp_pwm_mux ddp_rsz display_recorder. Excluded with reason: ddp_dsi,
     ddp_ovl, ddp_rdma_ex, ddp_wdma_ex, ddp_mmp, ddp_disp_bdg (record API / BDG), ddp_path (3 record-API
     calls would put a link dependency inside a record-free slice; its header is still carried).
     v3 headers only - 'find drivers/misc/mediatek/cmdq -name .c' returns 0 - so 0082's single-client-API
     property is preserved and no v3 engine behaviour is claimed.
     Four marked adaptations, each the minimal mainline equivalent of a v5.15 removal, and nothing else:
       cmdq_helper_ext.h struct timeval savetv -> struct timespec64 (grep -rn savetv over the tree = the
         declaration only); cmdq_mdp_common.h submit_tm/end_tm likewise (storage only);
       display_recorder.c do_gettimeofday(&t) -> ktime_get_real_ts64(&t) with (t.tv_sec & 0xFFF)*1000000
         + t.tv_nsec/1000, preserving the vendor's microsecond arithmetic and its wall-clock source;
       ddp_debug.c: three new struct proc_ops instances (dispsys_proc_ops, debug_dump_proc_ops,
         low_power_cust_proc_ops) used ONLY by the three proc_create() calls; the debugfs entries keep
         the vendor struct file_operations, which is why both name sets are present;
       ddp_log.h replaced by the vendor's full 112 lines (0081 had trimmed it to 51) because ddp_mutex.c
         needs DISP_LOG_W (:42) and DDPDUMP (:69).
       ddp_irq.c arch_counter_get_cntvct() -> __arch_counter_get_cntvct() (same CNTVCT read without the
         wrapper mainline does not expose to drivers); feeds a DDPAEE diagnostic string only.
     Gate (compile, not link): in the published tree, object directory cleared then
     make -j2 ARCH=arm64 CROSS_COMPILE=aarch64-buildroot-linux-gnu- drivers/misc/mediatek/video/mt6768/
     dispsys/ -> rc=0, zero errors; all 14 .o present and non-empty (ddp_dump.o 465,024 B ... ddp_met.o
     64,080 B); nm: 0 duplicate link-visible (T/D/B/R) definitions across the slice; nm -u against the
     79,730 symbols defined in this partial tree (1,772 objects) - ddp_color_format.o fully resolved,
     ddp_mutex.o/ddp_rsz.o need cmdqRecWrite via ddp_reg.h macros, 7 unresolved belong to the excluded
     ddp_path.o, remainder are L3/L4 providers (ddp_get_module_num, primary_display_diagnose,
     disp_helper_get_option, mtkfb_*). No vmlinux link is claimed: the sandbox builds 1,772 of ~7,400
     objects on 2 CPU / 3 GB, so the slice is compile-verified and deliberately NOT self-linking.
     Reproduction: git am of 0001-0084 onto 0996e0926 -> tree 3fa1c650082e917773ac00d2190befb35d575572
     == built tree, dirty 0; 0001-0083 prefix still -> 1bbd779ea9182f344c9e231621bca0ae8b715dae.
     NOT functional on hardware; no display output, panel, or backlight behaviour is claimed.


PUBLICATION NOTE (0084, after the fact, recorded here rather than silently): the commit message text of
0084-*.eml was corrected after the patch was generated - the count went from 13 to 14 objects (the
generated Makefile always listed ddp_drv.o, my enumeration did not) and a wrong claim about ddp_drv.o's
externals was replaced with the nm-measured one (cmdqBackupAllocateSlot/ReadSlot/WriteSlot, live and
unguarded; no bdg_* references at all). The patch payload was NOT touched: 91 files / 27,994 insertions
before and after, matching the diffstat the file itself states. The gate could not be re-run the day it
was edited (the build environment had just been lost to a sandbox reset, report/sandbox-reset-2026-09-05.md);
it has since been re-run in full after rebuilding that environment, and it passes: 0001-0084 -> tree
3fa1c650082e917773ac00d2190befb35d575572 dirty=0, and the slice build in that reproduced tree gives
rc=0 with 0 error: lines, 14/14 objects present and non-empty, 248 link-visible (T/D/B/R) definitions
with 0 duplicates. See report/l2-recovery-and-record-probe.md and build.json
gates.l2_recovery_recheck45. Sizes are quoted per checkout - they differ by 0-8 B between the amgate84
run and this one because __FILE__ is the build path - so the invariant is the object SET, not the bytes.


0085 video/mt6768: give videox the include set its ported object needs, so the whole tree builds
     Build fix, no source file touched. drivers/misc/mediatek/video/Makefile descends BOTH
     mt6768/dispsys/ and mt6768/videox/, and videox/ has carried `obj-y += disp_helper.o` since 0081
     with a 3-path ccflags list. 0084 replaced dispsys/display_recorder.h with the vendor file, which
     includes "mmprofile.h" (drivers/misc/mediatek/mmp/) - and behind it ion.h
     (drivers/staging/android/mtk_ion/) via ddp_info.h. From 0084 on, therefore,
     `make ARCH=arm64 drivers/misc/mediatek/video/` failed with `fatal error: mmprofile.h: No such file
     or directory`, while every gate that named only .../dispsys/ stayed green: the dispsys Makefile
     advertises mmp/, videox's did not. Fixed by generating videox/Makefile with the same include set
     l2slice.py computes for dispsys (stock dispsys + stock videox -I lists, filtered to existing
     directories), keeping obj-y += disp_helper.o.
     Gate (compile, not link), in the tree this patch is the tip of: object directories cleared, then
     make -k ARCH=arm64 CROSS_COMPILE=aarch64-buildroot-linux-gnu- drivers/misc/mediatek/video/
     drivers/mailbox/ drivers/soc/mediatek/ drivers/misc/mediatek/smi/ drivers/misc/mediatek/m4u/ ->
     rc=0, 0 error: lines, 15/15 expected objects present and non-empty (the 14 dispsys objects
     unchanged at their 0084 sizes, plus disp_helper.o 72,904 B), 257 link-visible (T/D/B/R)
     definitions with 0 duplicates across BOTH directories, and the 3 disp_helper_* names that the
     0084 census listed as "provider landed, not built" now have providers in-tree. 107 warning: lines,
     of which 105 come from the v3 headers carried in 0084 (cmdq_record.h 60, cmdq_helper_ext.h 45) and
     1 from disp_helper.c:290 -Wimplicit-fallthrough in vendor code as it stands (the
     DISP_OPT_PERFORMANCE_DEBUG case has no break) - seen for the first time because this is the first
     build that ever compiled that file; left alone, and CONFIG_WERROR=n so not fatal.
     Process fix that came out of it, so this class cannot hide again: l2-slice-gate.sh now builds the
     PARENT drivers/misc/mediatek/video/ and derives the expected object list from the obj-y of every
     landed Makefile in the slice (dispsys + videox), not from one hand-named directory; undeps.py
     accepts several object directories so a symbol satisfied by a sibling is not counted as a blocker.
     Reproduction: git am of 0001-0085 onto 0996e0926 -> tree 01e8b1eae19a33684b8769415ba16647d1de452a
     == built tree, dirty 0; 0001-0084 prefix still -> 3fa1c650082e917773ac00d2190befb35d575572.
     NOT functional on hardware; nothing about display output, panel or backlight behaviour is claimed,
     and no vmlinux/Image link is implied by a per-directory build.


0086 wait: state the __sched dependency in the header that has it, instead of in the most-included header
     Build fix for a defect 0001 carried in, found by the first whole-tree build this series has had since
     before 0082. The vendor `include/linux/wait.h:10` includes `linux/sched/debug.h`; 0001 carried that
     line, and in 5.15 it puts mainline's `static inline void show_state(void)` into every translation
     unit, where `drivers/acpi/fan.c`'s `DEVICE_ATTR_RW(state)` defines a static `show_state` with other
     types -> `error: conflicting types for 'show_state'`, and `make ARCH=arm64
     drivers/misc/mediatek/video/ ... vmlinux` dies at 1,732 objects. Neither colliding file is ours
     (`git diff --name-only 0996e0926..HEAD -- drivers/acpi/ include/linux/sched/debug.h` is empty) and it
     is not upstream either (pristine v5.15.220 with the same .config builds fan.o: rc=0, 87,160 B), so
     the port causes it. The line is load-bearing, and naively deleting it fails at `make prepare`
     (`pagemap.h:621: expected ';' before 'void'` + implicit declaration of wait_on_page_bit): __sched is
     defined only in sched/debug.h, and the vendor delta added it to pagemap.h 0->6, mm/filemap.c 0->2,
     kernel/sched/wait.c 0->3. So pagemap.h now includes what it uses and wait.h drops the line, returning
     BYTE-IDENTICAL to mainline - the port's footprint shrinks by one core header.
     Rejected instead: reverting the 11 vendor __sched annotations (also compiles, but __sched is
     __section(".sched.text"), which in_sched_functions() keys on when classifying a stack trace - not
     behaviour-neutral, kept as an upstream-cleanup candidate, not used as a build unblock); patching
     mainline's fan.c (backwards). Blast radius of the collision was one file in the whole tree
     (`grep -rln "static ssize_t show_state(" drivers/ fs/ net/`).
     Verified: whole tree compiles with 0 error: lines (`make -k ARCH=arm64 -j2 vmlinux`, 643 s from a
     2,684-object cache, `portwork/logs/full-k-10-vmlinux.log`); drivers/acpi/fan.o builds (87,200 B);
     `make prepare` 0 error lines. Verified in a scratch tree whose two files are `cmp`-identical to this
     patch's, which matters because might_sleep() embeds __LINE__ from that header. The remaining link
     failure in that same build (507 undefined references) is 0084's display slice, addressed by 0087 -
     not something this patch causes or fixes.
     NOT functional on hardware; no display, panel or backlight claim; no new symbol, no ABI change.


0087 video/mt6768: put the landed display objects behind CONFIG_MTK_DISP_BRINGUP, as the vendor gates this
     directory
     The 0085 tip compiled the whole tree and then failed to LINK: 507 `undefined reference` lines, every
     one from the 15 objects 0084/0085 landed (ddp_mmp_get_events, ddp_get_scenario_name/list,
     g_mobilelog, cmdqRecWrite, cmdqBackup{Allocate,Read,Write}Slot, DSI_*, disp_aal_on_end_of_frame,
     ovl_*, rdma_set_color_matrix, ddp_path_*, primary_display_*). That provider gap is the one the
     compile gate had already counted (87 names with no in-tree provider); what made it fatal was the
     *shape*: `obj-y` demands those objects from every build, so from 0084 on no vmlinux and no image
     could be produced at all - a regression of the property this project keeps, invisible to every gate
     because no gate had ever attempted a link.
     The vendor tree does not have that problem in one visible line:
     `drivers/misc/mediatek/video/mt6768/Makefile:20` is `obj-$(CONFIG_MTK_FB) += dispsys/` - the whole
     directory is demanded only when the display stack is enabled (and on this board MTK_FB=n, so stock
     even does not build the legacy display core either). This patch adopts that model with a port-local
     symbol instead of reusing MTK_FB (which would pull in the unported legacy fb path wholesale):
       config MTK_DISP_BRINGUP  (bool, depends on MTK_M4U, DEFAULT N)
     and the two landed Makefiles become `obj-$(CONFIG_MTK_DISP_BRINGUP) += <file>.o` (14 lines dispsys,
     1 videox). Both files are `l2slice.py` output, and the tool now emits the gated form by default
     (`--gate MTK_DISP_BRINGUP`, `--gate ""` for a bare obj-y probe), so the shape cannot drift from a
     hand edit. The Kconfig help text states the reason and says plainly that enabling it does not make
     display output work.
     Gate, both directions, measured (config: make defconfig + dev/even.fragment + dev/even-hardware.fragment,
     .config sha256 d780d6d3d391 - the sha256-12 that the tree still carries, verified now against both
     buildfull/.config and buildpub/.config; the pre-0087 config of record was 758ae54339bf, and this move
     is legitimate drift-caught: 0087 adds a Kconfig symbol, so olddefconfig writes a
     "# CONFIG_MTK_DISP_BRINGUP is not set" line. The 12 hex digits are the length measured):
       * gate ON  (`make CONFIG_MTK_DISP_BRINGUP=y drivers/misc/mediatek/video/`): rc=0, 0 error: lines,
         15/15 objects present and non-empty, 107 warning: lines (the same attributed set as 0085) - the
         per-slice compile check loses no strength.
       * gate ON  + link: still 507 undefined references, which is the control showing the gate, not some
         other change, is what makes the tree link.
       * gate OFF (the default any user builds): `LD vmlinux` succeeds - vmlinux 167,987,640 B,
         System.map 6,878,442 B - and `make Image.gz-dtb` yields Image 34,091,520 B,
         Image.gz 11,710,573 B, Image.gz-dtb 12,204,090 B with 0 error: lines. Appended DTB payload
         12,204,090 - 11,710,573 = 493,517 B, the size recorded for build-37; mt6768.dtb 122,474 B,
         sha 34a7e6b536a3, unchanged since build-33, so 0078-0087 edit no device tree.
         `nm vmlinux | grep -cE " T (disp_helper_|ddp_|display_recorder)"` = 0, as intended.
         Image/vmlinux sha256 is NOT comparable between rounds: the version string embeds `git describe`,
         so the same code from a different commit differs in those bytes - sizes and the DTB payload are
         the stable checks.
     No functional claim: enabling the switch compiles ported objects, it does not produce a first frame,
     and nothing is flashed or booted. 87 references still have no provider in the tree.
     Reproduction and the published-series gate, re-run afterwards from the .eml set alone (portwork/
     gate-published-0087.sh, numbers in report/build.json under l2_published_set_gate45): a fresh worktree at
     v5.15.220, `git am` of 0001-0087 -> rc=0, dirty=0, tree deba5bd29ec656ecb9b542837198cccc76cc5a09;
     prefixes 0001-0086 -> cfd5dc4f961ffe59cb6d69ddef1d58632303ea0f and 0001-0085 ->
     01e8b1eae19a33684b8769415ba16647d1de452a, both asserted by `publish.py --verify-only` (prefix depth is
     N - --count). In that reproduced tree: make prepare rc=0 / 0 error: lines; whole-tree link with the gate
     off rc=0, 0 error: lines, 0 undefined references over 3,693 objects, nm showing 0 display symbols and
     the providers still linked (mtk_iommu_v2_sys 130, SMI 5); make Image.gz-dtb rc=0, Image.gz-dtb
     12,204,094 B, appended DTB payload 493,517 B, mt6768.dtb 34a7e6b536a3 unchanged; with the switch forced
     on, 15/15 objects and 0 errors. That run also caught two defects in l2-slice-gate.sh itself (its
     expected-object sed matched only `obj-y`, so after this patch it checked 1 object and still said GREEN;
     and it counted untracked build output as dirt) - both fixed, with a cross-check on the object count.
     Read the counts, not just the verdict.
0088 soc/mediatek: provide the CMDQ backup-slot pool the landed display core calls, and nothing beyond it
     The 0087 tip left four cmdq names unresolved by the landed display objects; this supplies three of
     them and stops there, because the fourth (cmdqRecWrite, 29 link references from ddp_mutex.o and
     ddp_rsz.o) needs a struct cmdq_pkt, hence a cmdq_client, hence a mailbox channel, and nothing in this
     tree provides one. No #mbox-cells, no compatible string, no port-local GCE provider is added: that is
     a separate decision to be taken from stock evidence, not from convenience.
     What "backup slot" means in the v3 engine was measured first, because the name misleads:
     cmdqBackupAllocateSlot is cmdq_alloc_mem (v3/cmdq_record.c:3948) -> cmdqCoreAllocWriteAddress
     (cmdq_helper_ext.c:1996), a kernel buffer whose BUS ADDRESS is the handle, and the CPU read/write
     (cmdq_helper_ext.c:2072/:2159) resolve a handle by walking a global list under a spinlock and
     dereferencing plain memory. No GCE register, no mailbox - which is why this half lands alone. It does
     not claim the command queue can read the memory: PA vs M4U/SMI-translated IOVA stays an open hardware
     question, recorded in report/l2-record-layer-design-bprime.md.
     New files drivers/soc/mediatek/mtk-cmdq-disp-slot.c (+222) and
     include/linux/soc/mediatek/mtk-cmdq-disp-slot.h (+32), plus one Makefile line. Stock semantics are
     kept where a callsite could observe them (write returns the value it was given, not a status; a read
     that finds nothing gives 0 with rc 0; no barrier around the store; an out-of-range index is only
     dropped when no pool owns that address, and if a neighbour does, the access lands there - a hazard
     present in stock and deliberately not "fixed" into a silent divergence). The one intentional
     difference is widening the lookup offset from stock's s32 to long; identical for pools within 2 GiB,
     which is all this board can produce.
     Gated on CONFIG_MTK_DISP_BRINGUP in drivers/soc/mediatek/Makefile rather than a new symbol: a
     default-follows-symbol was measured not to follow an existing .config, and select could not be turned
     back off, so one switch for both is the shape that cannot desynchronise.
     Measured: gate off -> make vmlinux rc=0, 0 error and 0 undefined-reference lines, object not built,
     vmlinux still 167,987,640 B (identical to the 0087 tip), .config of record unchanged at
     d780d6d3d391 because no Kconfig symbol is added. Gate on -> 0 errors, 0 warnings from these files,
     object 57,256 B, whole-tree undefined references 507 -> 502 with cmdqRecWrite's 29 still present,
     each new symbol defined exactly once across all of drivers/**. Address arithmetic tested on the host
     against a transcription of the stock functions: upstream-port/tests/mtk_disp_slot_host_check.c, 37
     cases, 0 mismatches (report/mtk-disp-slot-check.txt).
     No functional claim: disp_probe_1() will allocate 5 slots (DISP_SLOT_NUM) and disp_get_ovl_bandwidth()
     will read them back, but with 502 references unresolved the display core still cannot be built into a
     usable kernel, and nothing is flashed or booted.
0089 misc: mediatek: give the panel bias rails their MT6370 provider
     0088 left the landed dispsys core calling two functions with no provider in the tree,
     display_bias_regulator_init() (ddp_drv.c:688) and disp_late_bias_enable() (:692). Both live in
     drivers/misc/mediatek/lcm/lcm_pmic.c, and that file cannot be landed alone: line 10 reads
     "#if defined(CONFIG_RT5081_PMU_DSV) || defined(CONFIG_MT6370_PMU_DSV)" and with neither symbol
     defined the compiler takes the #else - four "return 0" stubs, 140 of the 149 lines dropped. So
     this patch brings the provider as well as the consumer, and the config symbols that make them
     meet: 22 files, +4,972 lines, of which everything with code in it is copied verbatim from the
     4.19.325 vendor tree.
     The vendor directory is drivers/misc/mediatek/pmic/mt6370/v1/ - v1/, not the parent, because the
     parent Makefile descends into v1/ for CONFIG_MACH_MT6768=y (pmic/mt6370/Makefile:1-4), which is
     also the condition this port's config of record satisfies (apply.sh enables MACH_MT6768). An
     earlier round measured the top-level mt6370_pmu_dsv.c as an 885-line variant and never resolved
     which one the board builds; that ambiguity is what this line removes. Seven files land (i2c
     parent 380, rt-regmap glue 391, interrupt controller 401, MFD cell table 167, core child 281,
     DSV 583, DSV debugfs 246) with the four inc/ headers they actually include; the other five
     headers in that directory belong to the charger/fled/bled/rgbled sub-devices, which are not
     ported because nothing here references them.
     Branch selection goes through Kconfig rather than a -D on one file, and that was decided by a
     census of every CONFIG_* token in the ported files rather than by taste. CONFIG_RT_REGMAP turned
     out to be load-bearing: mt6370_pmu_regmap.c wraps its entire body in #ifdef CONFIG_RT_REGMAP
     (lines 12-370), so with the symbol absent mt6370_pmu_regmap_register() returns 0 without ever
     setting chip->rd, and mt6370_pmu_core.c:166 then calls rt_regmap_cache_reload() on a NULL handle
     - a driver that compiles, links and oopses at probe. Stock avoids that with "select RT_REGMAP"
     on MFD_MT6370_PMU, so the vendor's Kconfig text is kept verbatim and the board fragment
     (upstream-port/dev/even-hardware.fragment) now sets MFD_MT6370_PMU=y and MT6370_PMU_DSV=y, which
     is what even_defconfig:1687-1693 does. The other two branch points were checked and left alone:
     mt6370_pmu_i2c.c:177 tests "!defined(CONFIG_MTK_GPIO) || defined(CONFIG_MTK_GPIOLIB_STAND)",
     neither symbol exists here or in even_defconfig, so both trees take the same of_get_named_gpio()
     path; DEBUG_FS is y here and not set in even_defconfig, so the DSV debugfs node is the one place
     this port exposes more than stock (246 debug-only lines, no hardware access).
     No regulator-core change was needed, contrary to the plan of record: 5.15's
     regulator_dev_lookup() already falls through to regulator_lookup_by_name() -> regulator_match()
     -> rdev_get_name(), which prefers constraints->name, and the landed DT sets regulator-name =
     "dsv_pos"/"dsv_neg" on the two DSV cells - so regulator_get(NULL, "dsv_pos") resolves without
     porting an of_regulator_match() alias hook in the 0075 style.
     Measured in a tree at this landing commit (7320325c38fd188de726f3ba658d0f6b80e7eb6), board config
     of record sha256 prefix 099cdd6421b6. Display switch off: make vmlinux rc=0, 0 compile errors, 0
     undefined references, vmlinux 168,340,520 B and Image.gz-dtb 12,228,271 B (0088: 167,987,640 and
     12,204,087) - the growth is the sub-PMIC and rt-regmap now being board code, and nm confirms
     mt6370_pmu_regmap_register in that vmlinux with zero hits for display_bias_regulator_init, i.e.
     provider in, display consumer out. Display switch on: all 9 objects, 0 compile errors and 0
     diagnostics of any kind in the new files, whole-tree undefined-reference lines 502 -> 499 and
     distinct names 80 -> 78 with the closed pair exactly the two bias names and no new name
     introduced; 39 distinct global text symbols, none defined elsewhere in the tree. mt6768.dtb is
     byte-identical to 0088's (122,474 B, sha256 prefix 34a7e6b536a3) because no DT edit was made.
     What it does not buy, and why the maturity language stops where it does: the driver binds nothing
     yet. The appended DTB is built from arch/arm64/boot/dts/mediatek/mt6768.dts, which carries the
     MT6370 configuration node (mt6370_pmu_dts, with mt6370,intr_gpio = <&pio 3 0> and the DSV cells
     named dsv_pos/dsv_neg) but no I2C client under i2c5@11016000; subpmic_pmu@34 is in
     arch/arm64/boot/dts/oplus6768_20761/cust.dtsi:151, a board file landed for parity that this tree
     does not compile (no .dts in that directory), and the landed i2c5 node has neither
     #address-cells/#size-cells nor the bus's clock-frequency = <3400000>/mediatek,use-push-pull.
     Whether the port grows the SoC dts or adopts a board dts as its DT surface of record is an open
     architectural question and is not decided here; the vendor driver needs no edit for either answer,
     since its of table already matches "mediatek,subpmic_pmu" (mt6370_pmu_i2c.c:342-347). Nothing is
     flashed, nothing is booted, and no rail is claimed to move.
0090 video: mt6768: land the display path/scenario layer the manager already calls
     0084 landed the caller and 0089 left the callee: ddp_manager.c calls ddp_connect_path(),
     ddp_disconnect_path(), ddp_check_path() and ddp_path_init() and none of them had a definition
     anywhere in the tree, because the file that holds them - drivers/misc/mediatek/video/mt6768/
     dispsys/ddp_path.c, 987 lines and 24,946 bytes, copied verbatim from 4.19.325 - was the one
     landed display object too big for the earlier batches. This patch brings it with one Makefile
     line (obj-$(CONFIG_MTK_DISP_BRINGUP) += ddp_path.o), no edit to the file, and no new Kconfig
     symbol, so the single switch added in 0087 still covers it.
     Why this was the next slice and not the record layer: the 0089 census listed 78 distinct open
     names and 15 of them are exactly what this file exports, so landing it closes 15 while opening
     three (cmdqRecWrite, cmdqRecWaitNoClear, cmdqRecSetEventToken) that the record slice answers.
     Measured the same way as every other slice - nm -u over the built objects, not grep attribution.
     Gate, in a tree at this landing commit (a2985225f, HEAD^{tree} 7fbaf8257bfa9a33b6909c6ea4cfc1f2
     b17269ed), board config of record sha256 prefix 099cdd6421b6. Display switch OFF, whole-tree
     link: rc=0, two LD vmlinux steps, 0 error: lines, 0 undefined references, vmlinux 168,340,520 B,
     Image.gz 11,734,748 B, Image.gz-dtb 12,228,265 B, appended DTB payload 493,517 bytes - the
     payload has not moved since 0081 because no device tree file is touched here - and ddp_path.o
     absent with the switch off, 0 gated display objects, none of the four sample names in vmlinux.
     Display switch ON: 15 objects (the 14 landed ones plus ddp_path.o, 162,296 B), 0 error: lines,
     0 diagnostics attributed to ddp_path.c, whole-tree undefined-reference lines 486 -> 281 and
     distinct open names 78 -> 65; all 15 claimed names come out open:0 defined:1; 20 new global
     text/data symbols with 0 collisions against the rest of the tree; 0089's bias names stay closed.
     What it opens, and that is the point of the next patch: cmdqRecWrite (from DISP_REG_SET/DDP_REG
     in ddp_reg.h:266-280 as expanded by ddp_module.c, ddp_dpi.c and ddp_ddp.c) and
     cmdqRecWaitNoClear / cmdqRecSetEventToken (ddp_path.c:908, :910, :927). Those three were already
     open before this patch - the same file at :907/:924 also needs primary_display_is_video_mode(),
     which stays open with the panel handover it belongs to, and KNOWN-ISSUES 13 is unchanged.
     Seven warning: lines appear when the display objects are recompiled from scratch
     (cmdq/v3/cmdq_record.h:804/833/845/889, cmdq_helper_ext.h:880/881/988, all "declared inside
     parameter list"); they come from headers landed for the include closure, reached through
     ddp_log.h -> ddp_debug.h -> ddp_dump.h -> ddp_path.h, and they are not attributed to this file.
     Maturity language stops where it always has: this makes the path layer link, it does not make
     the display work. Nothing is flashed, nothing is booted, and no frame is claimed.
0091 soc: mediatek: answer the three record-mode symbols the display path layer calls
     0090 landed the caller of the record API; this patch lands the provider, and it is the narrow
     shape the design doc argued for (report/l2-record-layer-design-bprime.md), not the vendor
     engine. One new file, drivers/soc/mediatek/mtk-cmdq-disp-record.c (440 lines, 3 exported
     symbols), one header of pure encoding rules
     (include/linux/soc/mediatek/mtk-cmdq-disp-record.h, 229 lines), one Makefile line keyed on the
     existing CONFIG_MTK_DISP_BRINGUP. No new Kconfig symbol, no edit to drivers/mailbox/ or
     include/linux/mailbox/, no new device tree property, compatible, or #cells change - and the
     gate proves the last two in the negative: appended DTB payload 493,517 B and mt6768.dtb sha
     34a7e6b536a3, both identical to 0090's.
     Each entry point is the vendor's own function, read at its line: cmdqRecWrite() reproduces
     cmdq_op_write_reg() (v3/cmdq_record.c:1368) - mask select, opcode select, the
     cmdq_var_data_type() tag check at :196 - over the subsys resolution and field packing of
     cmdq_append_rw_s_command() (:847), and then delegates to this tree's cmdq_pkt_write_s_value() /
     cmdq_pkt_write_s_mask_value(); cmdqRecWaitNoClear() and cmdqRecSetEventToken() are delegations
     in the vendor too (:1510, :1532 -> cmdq_pkt_wait_no_clear(), cmdq_pkt_set_event()), so their
     shape here is not an approximation of anything, it is what stock does. The handle type is the
     vendor's own struct cmdqRecStruct (v3/cmdq_helper_ext.h:687, landed for the include closure), and
     the guard order is cmdq_check_before_append()'s (:694): no handle or no packet -EFAULT, a
     finalized record -EBUSY, an event with no id -EINVAL.
     Why the delegation is not a substitution: upstream-port/tests/mtk_disp_record_host_check.c builds
     the instruction words both ways for the addresses this tree can produce and every mask
     DISP_REG_SET can pass, and reports 55 cases / 0 mismatches. It parses the instruction numbers out
     of both trees' include/linux/mailbox/mtk-cmdq-mailbox.h rather than trusting a note - port
     MASK 0x02 equals vendor MOVE 0x02, and port WRITE_S_MASK 0x91 equals vendor WRITE_S_W_MASK 0x91,
     so the two-instruction masked write this adapter emits is the two-instruction masked write the
     vendor emits, word for word (the harness prints both 64-bit words for every case). Its one
     transcription - mtk-cmdq-helper.c's private struct cmdq_instruction and its two write_s helpers -
     is pinned by a sha256 of those bodies (fb9672f3187f, 8b965134cef7, 37d6ddcf5659) in the gate log,
     so a future edit shows up as a changed hash instead of a silent divergence.
     Address handling is stock's table, not an invention: the 44 name rows are the name column of
     v3/cmdq_subsys_common.c:9-68 (the harness compares them row by row, both directions, against the
     vendor file), read from this board's "gce" node with cmdq_dev_get_subsys_by_name()'s derivation
     (msb = cell[0] & cell[2], subsysID = cell[1], mask = cell[2], v3/cmdq_device.c:264). The harness
     also checks the device tree has not drifted: 31 of those 44 rows are present on this board and
     every triple is byte-equal to the vendor board's, which is what resolves mmsys_config to 1 and
     disp_dither to 2. Event ids follow cmdq_core_init_dts_data()'s rule (v3/cmdq_helper_ext.c:2265)
     for all 1023 indices, so the two SW tokens ddp_path.c passes self-number (640, 641) while
     CMDQ_EVENT_MUTEX0_STREAM_EOF takes stream_done_0 = <130> from the gce node - a property this
     board's DT already carries.
     Three omissions, each measured and each loud rather than silent. (1) An address no row covers is
     rejected with -EINVAL instead of taking the vendor's detour through CMDQ_SPR_FOR_TEMP with a
     LOGIC-assign instruction, because this tree has neither an SPR allocator nor a helper primitive
     for it; the harness carries as its own case the reason rejecting matters - truncating 99 into the
     5-bit field would have retargeted the write to subsys 3. Measured, the addresses that need the
     detour (0x1100e000, 0x1100d000) cannot be reached through this entry point from landed code, and
     video/mt6768 and video/common contain zero references to CMDQ_REG_VALUE, CMDQ_REG_EXT_VALUE or
     cmdq_reg_val_to_reg_str(). (2) Register-typed operands are refused: cmdqRecWrite() takes "u32
     value" here (v3/cmdq_record.h:167), so the vendor's 62-bit tag is 0 by construction and the type
     1 branch is unreachable - kept as a rejection so a later cmdqRecWriteFromDataRegister() finds a
     documented hole rather than a wrong encoding. (3) The vendor's prefetch policy
     (cmdq_get_func()->shouldEnablePrefetch(), around cmdq_append_command() at :970) is not carried,
     so a record built here omits the prefetch-disable and mark instructions a stock record with
     prefetch enabled would contain; that is a genuine difference in the instruction stream and it is
     the reason this layer's claim stops at "compiles and links".
     What it buys, measured in this slice's gate (series commit 42960308a, HEAD^{tree}
     3483759c24eb022373a5290523933b61bbd7ac62, config of record sha256 prefix 099cdd6421b6): switch
     OFF, whole-tree link rc=0 with 0 error: lines and 0 undefined references and vmlinux unchanged at
     168,340,520 B (Image.gz is 1 B off 0090's, from gzip and the build path in the version string, not
     from anything in this patch - the DTB payload and vmlinux are identical); the record object and all
     three symbols absent from that vmlinux, as the switch requires. Switch ON, the object compiles with
     0 error: lines and 0 diagnostics attributed to either new file (the 8 warnings it inherits are
     cmdq_record.h:804/833/845/889, cmdq_helper_ext.h:880/881/988 and mtk-cmdq-mailbox.h:91, all in
     headers landed before this patch), and the whole-tree link drops 65 -> 62 distinct open names
     (281 -> 211 reference lines) with each of the three closed names defined exactly once tree-wide and
     0 collisions from its 3 new globals; 0090's 15 path names and 0089's two bias names stay closed.
     The table is read once from a postcore_initcall rather than lazily at the callsite, because stock
     does it at its own driver's probe and a lazy of_mutex path in front of code the vendor reaches from
     atomic context would be a new sleeping bug; a record built before that initcall returns -EAGAIN.
     No maturity change: nothing in this tree can create a record (cmdqRecCreate() is referenced only
     by the unused DISP_REG_VAL_SET() macro at ddp_reg.h:272), so the layer is link-required and
     unreachable at runtime, no device is flashed, and the display path still has 62 names to go before
     it links, starting with the rdma/wdma pair in 0092 and the panel handover that is not a code
     problem.
